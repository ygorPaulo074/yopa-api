# data/

Esta pasta é gerada automaticamente em runtime e **não deve ser versionada**.

Contém os contextos dos agentes, scores de análise local e insights gerados pela IA.

A localização desta pasta é configurável via `DATA_PATH` no `.env`. O valor padrão é `./data` (raiz do projeto).